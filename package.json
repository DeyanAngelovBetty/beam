{
  "name": "beam-monorepo",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "npm run dev -w apps/sunlight",
    "storybook": "npm run storybook -w packages/beam",
    "typecheck": "tsc -b",
    "build": "npm run typecheck && npm run build --workspaces --if-present",
    "build-storybook": "npm run build-storybook -w packages/beam",
    "build:pages": "npm run build && npm run build-storybook && node scripts/assemble-pages.mjs"
  },
  "devDependencies": {
    "@types/node": "^22.20.1",
    "typescript": "^5.7.0"
  }
}
