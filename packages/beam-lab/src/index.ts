// @betty/beam-lab — the Theme Lab, product-agnostic. Public API is the drawer; the sheet,
// suggestions, and probe helpers are internal. Mount it in an app's shell with `product` +
// `jurisdiction` (see apps/gaspar + apps/sunlight App).
export { ThemeLabDrawer } from './ThemeLabDrawer';
